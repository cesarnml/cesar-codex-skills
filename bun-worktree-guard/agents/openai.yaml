interface:
  display_name: "Bun Worktree Guard"
  short_description: "Bootstrap fresh Bun worktrees"
  default_prompt: "Use $bun-worktree-guard to bootstrap this fresh Bun worktree before deeper repo work starts."
